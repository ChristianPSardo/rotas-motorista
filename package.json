{
  "name": "rotas-motorista",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "android:init": "npm run build && npx cap add android && npx cap sync android && node scripts/configure-native.mjs",
    "android:sync": "npm run build && npx cap sync android && node scripts/configure-native.mjs",
    "android:open": "npx cap open android",
    "android:apk": "node scripts/build-android.mjs",
    "ios:init": "npm run build && npx cap add ios && npx cap sync ios && node scripts/configure-native.mjs",
    "ios:sync": "npm run build && npx cap sync ios && node scripts/configure-native.mjs",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/core": "8.5.1",
    "@capacitor/ios": "8.5.1",
    "@capgo/background-geolocation": "8.4.3",
    "@capacitor/app-launcher": "8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.1",
    "typescript": "^5.9.2",
    "vite": "8.2.2"
  }
}
